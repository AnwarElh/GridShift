---
import GameView from '../../../views/Game.astro';
import { getGame } from '../../../lib/content';

const game = await getGame(Astro.locals, 'fr', Astro.params.slug!);
if (!game) return Astro.rewrite('/fr/404');
---
<GameView game={game} />
