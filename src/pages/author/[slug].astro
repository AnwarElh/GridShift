---
import AuthorPage from '../../views/AuthorPage.astro';
import { getAuthor } from '../../lib/content';

const author = await getAuthor(Astro.locals, 'en', Astro.params.slug!);
if (!author) return Astro.rewrite('/404');
---
<AuthorPage author={author} />
