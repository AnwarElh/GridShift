---
import Section from '../../../views/Section.astro';
import { sectionFromSlug } from '../../../i18n/config';

const section = sectionFromSlug('de', Astro.params.section!);
if (!section) return Astro.rewrite('/de/404');
---
<Section section={section} />
