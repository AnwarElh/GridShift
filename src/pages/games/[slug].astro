---
import GameView from '../../views/Game.astro';
import { getGame } from '../../lib/content';

const game = await getGame(Astro.locals, 'en', Astro.params.slug!);
if (!game) return Astro.rewrite('/404');
---
<GameView game={game} />
